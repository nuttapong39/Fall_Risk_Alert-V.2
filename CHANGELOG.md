# Changelog

รูปแบบเวอร์ชัน: `YYYY.MM.DD.HHMM` (วันที่+เวลาปล่อยรีลีส ตามเวลาที่ commit) —
ใช้เวลาต่อท้ายเพราะวันเดียวอาจปล่อยมากกว่า 1 รีลีส (ถ้าใช้แค่ `YYYY.MM.DD`
เฉยๆ รีลีสที่ 2 ของวันจะมีเลขเวอร์ชันชนกับรีลีสแรก ทำให้ปุ่ม "ตรวจสอบอัปเดต"
เข้าใจผิดว่าไม่มีอะไรใหม่)

**สำคัญ:** ทุกครั้งที่ commit อะไรที่มีผลกับ รพ. ที่ deploy ไปแล้ว
(โค้ด/schema/เอกสารที่ผู้ใช้เห็น) ต้องอัปเดตไฟล์ `VERSION` เป็นวันเวลาปัจจุบัน
เสมอ — ไม่งั้นระบบตรวจอัปเดตจะใช้งานไม่ได้ (เคยพลาดมาแล้ว: ปล่อยไป 7 commit
ในวันเดียวโดยไม่แตะ VERSION เลย ทำให้เครื่องที่อัปเดตแล้วกับเครื่องที่ยังไม่ได้
อัปเดตโชว์เลขเวอร์ชันเดียวกัน)

## 2026.09.04.1519
- **เพิ่ม `work-flow.md` — เอกสาร Work-Flow ของระบบ (อ่านก่อนเพิ่ม/แก้ feature ทุกครั้ง)**
  รวมกฎเหล็ก HOSxP ต้อง Read-Only เท่านั้น (SELECT อย่างเดียว ห้าม
  CREATE/INSERT/UPDATE/DELETE เด็ดขาด), pipeline มาตรฐานของ 1 Alert Module,
  Telegram mirror, การจัดการ secrets, ระบบเวอร์ชัน, และ Known Gaps/Security
  Backlog ที่ยืนยันสถานะกับโค้ดจริงแล้ว — เพิ่ม pointer ใน `CLAUDE.md` เพื่อการันตี
  ว่าถูกโหลดเข้า context ทุก session
  - พบระหว่างเขียนเอกสารว่า `CONTEXT.md`, `README.md`, `docs/PROJECT-STRUCTURE.md`
    ตกยุคตรงกัน — เขียนว่า "10 โมดูล" ทั้งที่มี 12 โมดูลทางคลินิกแล้ว (ขาด
    `lab_hemato`, `had` ที่เพิ่งสร้างก่อนหน้านี้) — แก้ทั้ง 3 ไฟล์ให้ตรงกันในคราวเดียว
  - แก้ประโยคใน README.md ที่อ้างผิดว่า Telegram mirror ครบทุกโมดูล (ที่จริง
    `had`/`lab_hemato` ยังไม่ mirror — บันทึกเป็น known gap ใน work-flow.md ด้วย)

## 2026.09.04.0007
- **แก้บั๊ก `moph_keys_admin.php` — ลบ Client/Secret Key แล้ว Save ไม่ว่างจริง (ด่วน)**
  ผู้ใช้รายงานว่าลบค่าในช่อง Client Key/Secret Key แล้วกด Save แต่ค่าเดิมยังอยู่ ไม่กลาย
  เป็นค่าว่าง — สาเหตุคือฟังก์ชัน `$keep()` ในหน้านี้ (จาก commit `c002cc0`) ถูกเขียนไว้ว่า
  "ช่องว่าง = คงค่าเดิม" เพื่อกันฟอร์มเก่าเขียนทับ key ทุกโมดูลพร้อมกันโดยไม่ตั้งใจ แต่ผล
  ข้างเคียงคือทำให้ลบ key ผ่านหน้าเว็บไม่ได้เลยแม้ตั้งใจลบจริง ต้องไปแก้
  `secrets/moph_keys.json` ตรง ๆ เท่านั้น
  - แก้จุดเดียว: ตัด fallback ไปค่าเดิมออกจาก `$keep()`/`$pair()` เหลือแค่บันทึกค่าที่
    submit มาตรงๆ — ใช้ pattern เดียวกับ `tg_*_chat` ที่มีอยู่แล้วในไฟล์เดียวกัน (ช่องว่าง =
    บันทึกว่างจริง)
  - ปลอดภัยเพราะทุกช่อง input pre-fill ด้วยค่าจริงจาก `secrets/moph_keys.json` เสมอ —
    ทางเดียวที่ฟอร์มจะ submit ค่าว่างได้คือผู้ใช้ลบเองโดยตั้งใจเท่านั้น
  - ทดสอบผ่าน session จริง 3 scenario (ค่าเดิม/ลบว่าง/ค่าใหม่) ยืนยันไม่กระทบโมดูลอื่นและ
    ไม่กระทบ `tg_*` เลย — ไม่มีการยิง Flex/MOPH จริงระหว่างทดสอบ (ทดสอบเฉพาะ
    `action=save_keys` เท่านั้น)

## 2026.09.03.2313
- **แก้ Auth Bypass (ด่วน) — 8 ไฟล์เปิด `auth_guard` กลับมาทำงาน**
  ผู้ใช้รายงานว่า `fracture_queue_ui.php` เข้าถึงได้โดยไม่ต้อง login — ตรวจสอบทั้งระบบแล้ว
  พบว่าไม่ใช่บั๊กเดี่ยว แต่เป็น `require_once auth_guard.php` ที่ถูกคอมเมนต์ปิดไว้แบบเดียวกัน
  ใน 6 หน้าเว็บ + 2 หน้า flex preview: `fracture_queue_ui.php`, `accident_queue_ui.php`,
  `covid_queue_ui.php`, `pharm_lab_queue_ui.php`, `lab_hemato_queue_ui.php`,
  `had_queue_ui.php`, `had_flex_preview.php`, `lab_hemato_flex_preview.php`
  - 4 ไฟล์แรกเกิดตั้งแต่ commit แรกสุดของ repo (bulk import) ส่วน 4 ไฟล์หลังเป็นไฟล์ที่สร้าง
    ในเซสชันก่อนหน้านี้เอง — ก็อปปี้ pattern ที่ผิดมาโดยไม่ทันสังเกต
  - ยืนยันก่อนแก้ว่าปลอดภัย 100%: ทั้ง 8 หน้าเป็นหน้าแสดงผลสำหรับคน login แล้วเท่านั้น
    ตัว worker จริงที่ยิง HOSxP/MOPH เป็นไฟล์คนละตัว ไม่ผ่านหน้าเหล่านี้เลย และรูปแบบเดียวกัน
    (`auth_guard` เปิดอยู่) ทำงานปกติอยู่แล้วใน 5 module อื่น (dengue/lepto/scrub/drug/sexual)
  - grep ทั้ง repo ยืนยันไม่มีไฟล์ไหน include/require/curl เรียกหน้าทั้ง 8 นี้แบบไม่ผ่าน browser
  - ระหว่างตรวจพบปัญหาที่ใหญ่กว่านี้อีกหลายชั้น (action.php หลายตัวไม่มี auth เลย, หน้า
    flex_preview ของ patient/fracture/pharm หลุด PHI คนไข้จริงมาตั้งแต่ต้น, `cron_covid_queue.php`
    ส่ง LINE จริงได้จาก URL เปล่า) — ผู้ใช้ตัดสินใจจำกัดขอบเขตรอบนี้เฉพาะ 8 ไฟล์ที่เป็นบั๊ก
    เดียวกับที่รายงาน ส่วนที่เหลือบันทึกไว้รอตัดสินใจแยกในรอบถัดไป

- **Restyle ปุ่ม "ส่งซ้ำทันที / Requeue / ล้าง Error" เป็น floating pill bar ใน 10 module**
  เดิม 9 module ใช้ sticky bar แบบเก่า (แสดงตลอดเวลา ไม่มีปุ่มยกเลิก, เด้ง dialog เดียวกัน
  ทุก action ไม่บอกผู้ใช้ว่านับเฉพาะแถวหน้าปัจจุบัน) และ `accident_queue_ui.php` ใช้อีกแบบ
  (fixed full-width strip) — ตอนนี้ทั้ง 10 ไฟล์ใช้ pattern เดียวกับ `had_queue_ui.php`/
  `lab_hemato_queue_ui.php`: `fracture_queue_ui.php`, `covid_queue_ui.php`,
  `pharm_lab_queue_ui.php`, `patient.php`, `drugitems01.php`, `dengue_queue_ui.php`,
  `sexual.php`, `Leptospira.php`, `scrubtyphus.php`, `accident_queue_ui.php`
  - **Safety invariant ที่ยืนยันก่อนแก้**: `*_action.php` ทุกไฟล์อ่านแค่ 3 field
    (`$_POST['action']`, `$_POST['ids']`, `$_POST['token']`) ไม่แตะ DOM/id/class เลย —
    การ restyle จึงเป็นแค่หน้าตา/JS ล้วนๆ ไม่กระทบ backend ไม่ต้องแตะ `*_action.php` แม้แต่ไฟล์เดียว
  - แก้ทีละไฟล์เรียงจากเสี่ยงน้อยไปมาก verify ครบก่อนไปไฟล์ถัดไป — ทุกไฟล์ผ่าน: `php -l`,
    diff ชื่อ field ก่อน/หลังเหมือนกันเป๊ะ, ทดสอบ JS จริงด้วย jsdom+jQuery (ไม่ใช่ stub ปลอม)
    ในแซนด์บ็อกซ์ Node ที่ `submit()`/`Swal.fire` เป็น spy function เปล่า **ไม่มีทาง
    network I/O หรือส่ง Flex เกิดขึ้นได้จริงระหว่างการทดสอบเลย**, ปิดท้ายด้วย curl ตรวจ markup
    จริงผ่าน Apache (auth cookie จริง)
  - **`dengue_queue_ui.php`, `sexual.php`, `Leptospira.php`, `scrubtyphus.php`** มีคอลัมน์
    ปุ่มส่งทีละแถว (`.btn-send-row` → AJAX `action=send_queue_item`) แยกจาก bulk bar โดยสิ้นเชิง
    — ไม่ถูกแตะเลย ยืนยันด้วย grep ก่อน/หลังว่าจำนวนเท่าเดิม
  - **`Leptospira.php`/`scrubtyphus.php`**: form ที่ bulk bar submit เดิมใช้ id ที่ก็อปมาจาก
    dengue ผิด (`dqActionForm`) ทั้งที่ endpoint จริงชี้ `lepto_queue_action.php`/
    `scrub_queue_action.php` ถูกอยู่แล้ว — เปลี่ยนแค่ id เป็น `lepForm`/`scrForm` ตามจริง
    ส่วน Sync-from-HOSxP modal ที่ก็อปผิดเหมือนกัน (`dqSyncModal` ฯลฯ) **ไม่ถูกแตะ** เพราะ
    อยู่นอกขอบเขตงานนี้ (คนละฟีเจอร์กับ bulk bar)
  - **`accident_queue_ui.php`**: เก็บ dialog ข้อความเฉพาะราย action ไว้ (ต้นแบบที่อีก 9
    module เอาไปใช้) + เก็บ tri-state `indeterminate` select-all ไว้ (ดีกว่า reference)
    เปลี่ยนแค่รูปทรงจาก fixed full-width strip เป็น centered pill ตาม pattern ใหม่
    และเพิ่ม disclaimer "(เฉพาะแถวในหน้าปัจจุบัน)" ที่ของเดิมไม่มี
  - `pharm_lab_queue_ui.php` มี auto-sync-on-load เมื่อ URL มี `?start=&end=` — ตรวจ verify
    ด้วย URL เปล่าเสมอเพื่อไม่ให้เขียน DB จริงโดยไม่ตั้งใจระหว่างทดสอบ
  - ไม่มีการ POST จริงไปที่ `*_action.php` และไม่มีการส่ง Flex/LINE จริงเกิดขึ้นเลยตลอดการทำงานนี้

## 2026.09.03.2137
- **Module ใหม่: HAD Alert (`had`) — High Alert Drug** — แจ้งเตือนคนไข้ที่รับยากลุ่ม
  เฝ้าระวังสูง เมนูอยู่ใต้ "งานเภสัชกรรม" ถัดจาก Hematocrit Alert ในกลุ่มงานสนับสนุน
  - **เงื่อนไขใช้ field type `codes` ที่มีอยู่แล้ว** (ไม่ต้องเพิ่ม UI ใหม่เหมือน
    Hematocrit Alert) — icode คั่นด้วย , ล้วน ไม่มีเงื่อนไขค่าตัวเลขซับซ้อน
  - **default = 17 icode จริงของ รพ. ไม่ใช่ placeholder** — ยืนยันจากข้อมูลจริงใน HOSxP
    ว่า `drugitems.name` ของทุก icode เหล่านี้มีคำว่า "[HAD]" ต่อท้ายชื่อยาจริง (เช่น
    "WARFARIN (สีฟ้า)[HAD]") ต่างจาก Hematocrit ที่ query ต้นแบบเป็น placeholder ชัดเจน
    ("ให้กรอกเองในหน้า...") กรณีนี้จึงปลอดภัยที่จะ seed ด้วยรายการจริงตั้งแต่แรก
  - `sources/had_source.php` — join `opitemrece` + `drugitems` + `patient` (แบบเดียวกับ
    `sources/drug_source.php` ที่มีอยู่แล้ว) แต่ไม่ต้องคำนวณ `visit_vn` แบบ drug เพราะ
    unique key ของ `had_queue` ใช้ `(hn, icode, vstdate)` ตรงๆ
  - **`had_queue` UNIQUE (hn, icode, vstdate) ไม่ใช่ (hn, vstdate) เฉยๆ** — ยืนยันจาก
    ข้อมูลจริงว่าคนไข้ 1 ราย รับยา HAD ต่างชนิดวันเดียวกันได้ (พบหลาย HN ที่ได้ warfarin
    สีฟ้า+สีชมพูพร้อมกัน) ถ้าใช้ `visit_vn` เดี่ยวแบบ `drug_queue` จะชนกันและเหลือ
    ยาแค่ชนิดเดียวต่อวัน — พลาดยาที่สองไปเงียบๆ
  - `HAD.php` เป็น worker ตัวจริง (ตั้งชื่อไฟล์ตามที่ระบุ) `run_had.bat` เรียกตรง
    แบบเดียวกับ `lab_hemato.php`/`pharm_lab.php` · `had_flex_preview.php` ใช้หน้าตา
    การ์ดชุดเดียวกับ `flex_editor.php` (ดีไซน์ปัจจุบัน)
  - accent การ์ด `#0E7490` (cyan) — hue สุดท้ายที่ยังไม่ชนใน 12 module ที่มีอยู่ทั้งหมด
  - ทดสอบแล้ว: worker รันจริงผ่าน Task Scheduler ที่เพิ่งติดตั้ง — ingest 112 รายการ,
    ส่งสำเร็จ 50/50 (MOPH ตอบ 200 ทุกใบ, ที่เหลือรอรอบถัดไปตาม batch limit)

## 2026.09.03.0911
- **Module ใหม่: Hematocrit Alert (`lab_hemato`)** — แจ้งเตือนค่าผลตรวจเลือดผิดปกติ
  เมนูอยู่ใต้ "งานเภสัชกรรม" ในกลุ่มงานสนับสนุน
  - **field type ใหม่ `labconds`** ในระบบ filter modal — ของเดิมมีแต่ textarea
    (`codes/results/single/int/patterns/rules`) ทำ UI แบบนี้ไม่ได้ ตัวใหม่เป็นฟอร์มที่
    เพิ่ม/ลบได้: 1 ฟอร์ม = 1 ชุด `lab_items_code` + เงื่อนไขหลายแถว · แต่ละแถวติ๊ก
    `<` `>` `=` ร่วมกันได้ (`<` กับ `=` = `<=`) แล้วกรอกค่าเอง · ในฟอร์มรวมด้วย OR
    ระหว่างฟอร์มก็ OR · ติ๊กครบ 3 = จริงเสมอ ระบบตัดทิ้งพร้อมเตือนบน UI
    - แยกเป็น `partials/filter_labconds.php` เพราะ DOM เป็นของ JS ทั้งก้อน
    - helper ใหม่ใน `module_filters_loader.php`: `mf_ops_to_sql()`, `mf_parse_labconds()`,
      `mf_labconds_clause()`, `mf_labconds_summary()`
  - `sources/lab_hemato_source.php` — รองรับทั้ง MySQL และ PostgreSQL · **guard ว่าผลเป็น
    ตัวเลขล้วนก่อน cast เสมอ** (PostgreSQL จะ throw ทั้ง query ถ้าเจอแถวที่ cast ไม่ได้
    แม้แถวนั้นจะไม่เข้าเงื่อนไข) · ค่าทุกตัว bind ไม่มี concat ลง SQL
  - `lab_hemato_queue.sql` — UNIQUE `(hn, lab_order_number, lab_items_code)` ต้องมี
    `lab_items_code` ในคีย์ด้วย เพราะ 1 ใบสั่ง lab มีได้หลายรายการตรวจ
  - `lab_hemato_queue_ui.php` — ปุ่มครบ: แก้เงื่อนไข · Sync จาก HOSxP · ดูตัวอย่าง Flex ·
    Dashboard · รายงานรายวัน
  - `lab_hemato_flex_preview.php` — ใช้หน้าตาการ์ดชุดเดียวกับ `flex_editor.php`
    (ดีไซน์ปัจจุบัน) แทนแบบเก่าของ `pharm_flex_preview.php` + validator สีในหน้าเดียวกัน
  - `lab_hemato.php` + `run_lab_hemato.bat` + `task/Lab_Hemato_Auto Sender.xml`
    (worker เรียก `lab_hemato.php` ตรง แบบเดียวกับ pharm_lab/covid/drug ที่ไม่มีไฟล์ `_ingest` แยก)
  - ต่อเข้าระบบเดิม: sidebar · `dashboard_modules.php` · `flex_theme`/`flex_themes.json`/
    `flex_builders`/`flex_editor` (accent `#9F1239` rose — hue ที่ยังไม่ชนกับ 10 module เดิม) ·
    `moph_keys_loader` + `moph_keys_admin` (5 จุด ไม่งั้นคีย์จะถูกลบตอน save)
  - **default ของเงื่อนไข = ว่างโดยตั้งใจ** → ไม่ดึงอะไรเลยจนกว่าจะตั้งค่าผ่านหน้าเว็บ
    ตอนทดสอบใส่รหัสตัวอย่าง `51` ไว้ แล้วพบว่าตรงกับรหัสจริงในระบบ คืนมา 163 เคส/7 วัน
    ซึ่งเป็นค่า RBC ไม่ใช่ Hematocrit — ถ้าปล่อยไว้ รพ. ที่เปิด Task Scheduler ก่อนตั้งค่า
    จะยิงแจ้งเตือนผิดทันที

## 2026.09.02.1518
- **แก้ตามรอบก่อน: โค้ดตัด BOM ใน `system_update_status.php` เขียนผิดจนไม่ทำงาน**
  รอบ 1515 เขียนเงื่อนไขเป็น `substr($raw,0,3) === "<BOM ตัวอักษรจริง>"` — ฝั่งขวาเป็น
  อักขระ 3 ตัว (U+00EF U+00BB U+00BF) ซึ่งเก็บใน PHP source ที่เป็น UTF-8 ได้ **6 ไบต์**
  ส่วนฝั่งซ้ายเป็น 3 ไบต์ จึงไม่มีวันเท่ากัน BOM เลยไม่เคยถูกตัดจริง
  - เปลี่ยนมาใช้ `strncmp($raw, pack('C3', 0xEF, 0xBB, 0xBF), 3) === 0` ซึ่งไม่พึ่ง
    escape sequence หรือ encoding ของ source file เลย
  - ทดสอบด้วยโค้ดที่ดึงจากไฟล์จริง (ไม่พิมพ์ใหม่) กับไฟล์ 3 แบบ: มี BOM -> `done`,
    ไม่มี BOM -> `running`, ไม่มีไฟล์ -> `idle` ผ่านทั้งหมด

## 2026.09.02.1515
- **แก้บั๊กร้ายแรง: `logs/update_status.json` อ่านไม่ผ่านมาตลอด เพราะ UTF-8 BOM**
  `update.ps1` เขียนไฟล์สถานะด้วย `Set-Content -Encoding UTF8` ซึ่ง **PowerShell 5.1
  ใส่ BOM (`EF BB BF`) ให้เสมอ** — `json_decode()` ฝั่ง PHP มองว่า BOM เป็นอักขระเกิน
  หน้า `{` จึง parse ไม่ผ่าน `system_update_status.php` เลยตกไป fallback ตอบ
  `status:'idle'` **ทุกครั้ง ทุกเครื่อง** ยืนยันด้วยการ hexdump ไฟล์จริงหลังอัปเดตสำเร็จ
  (`efbbbf7b2273`) แล้วรัน json_decode ซ้ำ -> `Syntax error`
  - นี่คือสาเหตุที่แท้จริงที่ progress bar เดิมไม่เคยเห็นสถานะจริงเลย จนต้องมีโหมดเดา %
    จากเวลามาคั่นไว้ — คอมเมนต์เดิมเขียนว่า "อ่านไม่ได้บนบางเครื่อง" ความจริงคือ
    อ่านไม่ได้เลยตลอดมา
  - และมันทำให้ฟีเจอร์ refresh อัตโนมัติที่เพิ่งทำใช้ไม่ได้ทั้งหมด เพราะหน้าเว็บรอเห็น
    `status=done` ซึ่งจะไม่มีวันมาถึง
  - `system_update_status.php`: ตัด BOM ทิ้งก่อน `json_decode` — จุดนี้สำคัญสุดเพราะ
    ทำงานทันทีกับไฟล์ BOM ที่ค้างอยู่แล้วบนเครื่องที่ deploy ไปก่อนหน้า
  - `task/update.ps1`: เปลี่ยน `Set-Content -Encoding UTF8` เป็น
    `[System.IO.File]::WriteAllText(..., UTF8Encoding($false))` ตัดต้นตอไม่ให้เกิด BOM
    ตั้งแต่แรก (BOM ของตัวไฟล์ `update.ps1` เองยังคงไว้ — จำเป็นต่อการอ่านภาษาไทย
    ของ PowerShell 5.1 ตามที่แก้ไว้ใน 2026.08.25.2229)

## 2026.09.02.1455
- **เปลี่ยนวิธีแสดงผลตอนอัปเดต: เปิด Terminal ให้เห็นจริง แทน progress bar**
  เดิมปุ่ม "อัปเดตตอนนี้" สั่ง `update.bat` ทำงานแบบ**ซ่อนหน้าต่าง** แล้วหน้าเว็บ poll
  `logs/update_status.json` มาวาดแถบ % ซึ่งเป็น hybrid ระหว่าง step จริงกับ % จำลอง
  จากเวลา (`verSimPct()` เพดาน 95%) — ซับซ้อนและผู้ใช้ไม่เห็นว่าเกิดอะไรขึ้นจริง
  เวลาพลาดก็ไม่เห็นสาเหตุ ตอนนี้เปิดหน้าต่าง Terminal ให้ดูของจริงจนจบไปเลย
  - `task/launch_detached.vbs`: เปลี่ยนจาก `WScript.Shell.Run(cmd, 0, False)` (0 = ซ่อน)
    เป็น `Shell.Application.ShellExecute(bat, "", "", "runas", 1)` — `1` = แสดงหน้าต่าง,
    `"runas"` = ยกสิทธิ์เป็น Administrator (ถ้า Apache elevated อยู่แล้วจะรันต่อทันที
    ไม่มี UAC prompt) ยังต้องผ่าน wscript เหมือนเดิมเพราะ `exec()` ของ PHP จะ *รอ*
    คำสั่งจบ ถ้าเรียก .bat ตรงๆ request จะค้างตลอดกาลที่คำสั่ง `pause`
  - `system_update_action.php`: เพิ่ม pre-flight ตรวจ **Session 0** ก่อนสั่งรัน — ถ้าเครื่องไหน
    ติดตั้ง Apache เป็น Windows Service มันจะอยู่ Session 0 ซึ่ง Windows กั้นขาดจากเดสก์ท็อป
    ทั้งหน้าต่าง Terminal และกล่อง UAC จะไม่มีทางโผล่ ผู้ใช้จะนั่งรอหน้าต่างที่ไม่มีวันมา
    จึงตรวจด้วย `tasklist /FI "PID eq <getmypid()>"` (เร็วกว่า PowerShell ~6 เท่า:
    52ms vs 321ms) แล้วแจ้งให้ไปคลิกขวา `task\update.bat` เลือก Run as administrator แทน
    — ตรวจ *ก่อน* เขียน `update_status.json` ไม่งั้นสถานะ `running` จะค้างไปบล็อกการกด
    ครั้งถัดไปผ่าน concurrent guard นานถึง 10 นาที
  - `settings.php`: ตัด progress bar + ปุ่ม "Update Success" ออกทั้งหมด
    (`verSimPct`, `VER_EXPECTED_MS`, `verIdleTicks`, `verLastMessage`, `verRunStartTime`)
    เหลือกล่องบอกให้ดูหน้าต่าง Terminal แทน · ยังคง poll `system_update_status.php`
    ต่อไปแต่ใช้เพื่อ **รอ `done` แล้วสั่ง `location.reload()` 1 ครั้ง** พอโหลดหน้าใหม่เสร็จ
    จะเด้ง popup "อัปเดตสำเร็จ" พร้อมเลขเวอร์ชันใหม่ให้
    - **กันลูป reload:** `verResumeFromStatus()` เช็คสถานะทุกครั้งที่หน้าโหลด และจะเห็น
      `done` ที่ยัง fresh (< 30 นาที) เสมอ ถ้าไม่กันจะ reload วนไม่รู้จบ — ใช้ `updatedAt`
      ของรอบนั้นเป็นลายเซ็นเก็บใน `sessionStorage` (`ckh-upd-done`) รอบไหน refresh ไปแล้ว
      จะไม่ refresh ซ้ำ ส่วนข้อความ popup เก็บแยกที่ `ckh-upd-msg` (ใช้แล้วลบทิ้ง)
      ทุกการเรียก `sessionStorage` ห่อ try/catch ไว้ (โหมด private / ปิด site data จะ throw)
  - `task/update.bat`: แก้คอมเมนต์หัวไฟล์ที่เขียนว่า "No Administrator/UAC needed" และ
    "it must NOT require a UAC prompt to work" ซึ่งขัดกับพฤติกรรมใหม่ · logic ไม่แตะ
    และคง `pause` ไว้ให้หน้าต่างค้างรอผู้ใช้อ่านผลก่อนปิด
  - `docs/update-guide.html`: ปรับขั้นตอน/ภาพ mockup/หัวข้อแก้ปัญหาให้ตรงของใหม่
    (เพิ่มเคส "ไม่มีหน้าต่าง Terminal เด้ง = Apache เป็น Windows Service") + ลบ CSS
    `.pbar-*` ที่กลายเป็น orphan

## 2026.08.25.2301
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบกดปุ่ม "อัปเดตตอนนี้" ผ่านหน้าเว็บจริง
  (เส้นทางใช้งานปกติตลอดไป) อีกรอบ หลังแก้ปัญหา robocopy ค้าง + BOM หาย + กดซ้อนกัน
  ไปแล้วทั้งหมดใน 2202/2229 — เครื่องที่เพิ่งกู้คืนด้วยการดับเบิลคลิก update.bat ตรงๆ
  จะได้ทดสอบ progress bar ผ่านปุ่มเว็บแบบครบวงจรจริงๆ

## 2026.08.25.2229
- **แก้บั๊กร้ายแรง: อัปเดตพังด้วย parser error ภาษาไทยเพี้ยนทั้งไฟล์**
  (`Missing closing ')'`, `The string is missing the terminator`, ฯลฯ) — ยืนยันแล้วว่า
  git blob และ GitHub raw ของ `task/update.ps1` มี UTF-8 BOM ถูกต้องเสมอ แต่บาง
  git client/checkout บน Windows (เจอจริงที่ รพ. อื่น) ทำ BOM หายระหว่างดึงไฟล์มาที่
  เครื่อง — PowerShell 5.1 เจอไฟล์ไม่มี BOM จะ fallback ไปอ่านด้วย ANSI code page ของ
  เครื่อง (เช่น รหัสภาษาไทย 874) ทำให้ตัวอักษรไทยทุกตัวเพี้ยนเป็น mojibake และบาง byte
  ที่เพี้ยนดันไปทับซ้อนกับอักขระโครงสร้าง (`"`, `(`, `{`) พอดี ทำให้ parser พังไปด้วย
  - `task/update.bat`: เพิ่มขั้นตอน self-heal ก่อนรัน — เขียนไฟล์ `update.ps1` ทับตัวเองด้วย
    UTF-8 BOM ที่ถูกต้องเสมอ (อ่านเนื้อหาแบบ UTF-8 ชัดเจนแล้วเขียนกลับพร้อม BOM) ก่อนสั่งรัน
    จริงทุกครั้ง — ปลอดภัย/idempotent (ถ้า BOM ถูกต้องอยู่แล้วก็ไม่มีผลอะไร) ทดสอบยืนยันแล้ว
    ว่าซ่อมไฟล์ที่ BOM หายให้กลับมาพาร์สผ่าน 0 error ได้จริง ผ่าน `.bat` เต็มรูปแบบ (cmd.exe)

## 2026.08.25.2202
- **แก้บั๊กร้ายแรง: อัปเดตค้างเงียบๆ ไม่ error ไม่ progress ต่อ (ตอนขั้นตอนสำรองข้อมูล)**
  ทั้ง 2 จุดที่เรียก `robocopy` ใน `task/update.ps1` ไม่เคยระบุ `/R` (จำนวนครั้ง retry)
  กับ `/W` (เวลารอระหว่าง retry) มาก่อน — ค่า default ของ robocopy คือ **retry 1,000,000
  ครั้ง รอครั้งละ 30 วินาที** ถ้าเจอไฟล์แม้แค่ไฟล์เดียวที่ถูกล็อกอยู่ (เช่น antivirus กำลังสแกน,
  หรือไฟล์ log ที่ script นี้เองกำลังเขียนอยู่พอดี) robocopy จะค้างรอเงียบๆ แบบดูเหมือนแฮง
  ตลอดกาล ไม่ error ไม่ progress ต่อ — ยืนยันสาเหตุด้วยการจำลองไฟล์ล็อกจริงบนเครื่อง dev
  (ยืนยันว่าไม่ระบุ `/R /W` ทำให้ค้าง, พอใส่ `/R:2 /W:2` แล้วล้มเหลวไว ~4 วิ แทน)
  - เพิ่ม `/R:2 /W:2` ให้ทั้ง 2 จุด (สำรองข้อมูล + คัดลอกไฟล์ทับ path ZIP) — ถ้าไฟล์ยังล็อก
    อยู่หลัง retry 2 ครั้ง จะ throw error ให้เห็นชัดเจนแทนที่จะค้างไม่มีกำหนด

## 2026.08.25.2130
- **แก้บั๊กใหม่: อัปเดตล้มเหลวแบบสุ่ม `Cannot find path '...\.gitignore' because it does not exist`**
  ระหว่างขั้นตอนคัดลอกไฟล์ (path ZIP) — สาเหตุที่เป็นไปได้มากที่สุดคือกดปุ่ม "อัปเดตตอนนี้"
  ซ้อนกัน 2 ครั้ง (เช่น รีเฟรชหน้าแล้วกดซ้ำระหว่างที่รอบแรกยังทำ ZIP download อยู่ ซึ่งอาจ
  ใช้เวลาหลายนาที) ทำให้ 2 โปรเซส `update.ps1` แย่งกันอ่าน/เขียนไฟล์ temp เดียวกัน — ไม่มี
  กลไกกันการสั่งซ้อนมาก่อนเลย
  - `system_update_action.php`: เพิ่ม concurrent-run guard — ถ้ามีสถานะ `running` สดๆ
    (ไม่เกิน 10 นาที) อยู่แล้ว จะปฏิเสธคำขออัปเดตใหม่ทันทีพร้อมข้อความแจ้งให้รอ แทนที่จะ
    ปล่อยให้ 2 โปรเซสชนกัน (เกิน 10 นาทีถือว่าสถานะค้าง ไม่ block ของจริงตลอดไป)

## 2026.08.25.2123
- อัปเดต `docs/update-guide.html` ให้ตรงกับฟีเจอร์ล่าสุด — เพิ่ม mockup progress bar
  + ปุ่ม "Update Success" (แทน mockup log ดิบอย่างเดียว), เพิ่มโน้ตเรื่องแจ้งเตือน
  LINE/Telegram อัตโนมัติหลังอัปเดตสำเร็จ, เพิ่มโน้ตว่ารีเฟรช/ปิดแท็บระหว่างรอได้ไม่มี
  ปัญหา (ระบบ resume สถานะให้อัตโนมัติ), เพิ่ม troubleshooting เรื่องแถบค้างที่ 90-95%,
  ปรับตัวอย่างเลขเวอร์ชันเป็นรูปแบบ `YYYY.MM.DD.HHMM` ให้ตรงปัจจุบัน (จากเดิม
  `YYYY.MM.DD` เก่า), เพิ่ม checklist ข้อตรวจสอบแจ้งเตือน LINE/Telegram

## 2026.08.25.2116
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบ hybrid time-based progress bar
  (2107) ให้ถูกต้อง **สำคัญ: ต้องกด F5 รีเฟรชหน้า settings.php ก่อนกดอัปเดต**
  เพราะรอบก่อนหน้าเห็นข้อความเตือนเวอร์ชันเก่า แปลว่าเบราว์เซอร์ยังโหลด JS ค้างจาก
  ก่อนอัปเดต ไม่ใช่ตัวที่เพิ่งดึงมาไว้ในเครื่อง

## 2026.08.25.2107
- **เพิ่ม progress bar แบบ hybrid — การันตีเห็นความเคลื่อนไหวเสมอ ไม่ว่าสถานะจริงจาก
  server จะอ่านได้หรือไม่:** ยังไม่ยืนยัน root cause 100% ว่า opcache (2054) แก้ปัญหา
  progress bar idle ตลอดของบาง รพ. ได้จริงหรือไม่ — เพิ่มชั้นป้องกันเพิ่มเติมที่ไม่ต้องพึ่ง
  การแก้บั๊กต้นตอให้เสร็จก่อน: คำนวณ % จำลองจากเวลาที่ผ่านไปจริงตั้งแต่กดปุ่ม (ประมาณ 4 นาที
  = 100%, เพดานที่ 95% ไม่ยืนยันว่าเสร็จจริงจนกว่าจะเจอสถานะ done จริง) แล้วโชว์ค่าที่มากกว่า
  ระหว่าง % จริงจาก server กับ % จำลอง — ถ้าสถานะจริงอ่านได้ (ปกติ) จะแม่นยำตามเดิม ถ้าอ่านไม่ได้
  (เครื่องที่มีปัญหาเฉพาะจุด) แถบจะยังขยับตามเวลาให้เห็นเสมอ ไม่ค้างว่างเปล่าอีกต่อไป
  ปรับข้อความเตือน idle ให้บอกตรงๆ ว่า "แถบกำลังประมาณจากเวลาที่ผ่านไปแทน"

## 2026.08.25.2054
- **สงสัยสาเหตุใหม่ของบั๊ก "progress bar ไม่ขึ้น" ที่เกิดซ้ำแม้แก้ frontend แล้ว (2041):**
  รพ. อื่นทดสอบอัปเดตไปที่ 2041 แล้วยัง idle-warning เหมือนเดิม ทั้งที่ log ยืนยันว่า
  `update.ps1` ทำงานถูกต้อง 100% เหมือนทุกครั้ง — สังเกตว่าอาการเดิมเป๊ะเกิดซ้ำข้ามหลาย
  commit ที่ genuinely แก้โค้ดคนละจุดกัน (ครั้งนี้คือ resume-polling ฝั่ง JS ไม่เกี่ยวกับ
  การเขียนไฟล์สถานะเลยด้วยซ้ำ) ลักษณะแบบนี้เข้าข่าย **PHP OPcache รันโค้ดเก่าค้าง**
  (ไฟล์บนดิสก์ถูกอัปเดตทับจริง แต่ bytecode ที่ compile ไว้ในหน่วยความจำยังเป็นเวอร์ชันเก่า
  ถ้าเซิร์ฟเวอร์ตั้ง `opcache.validate_timestamps=0` เพื่อ perf) — อธิบายได้ว่าทำไมแก้
  ถูกจุดแล้วแต่ runtime behavior เหมือนเดิมไม่เปลี่ยน
  - `system_update_action.php`: เพิ่ม `opcache_reset()` ทันทีที่ action=check/run ถูกเรียก
    (ผู้ใช้กดเอง ไม่ใช่ polling ที่ถี่ทุก 1.5 วิ กัน perf กระทบหน้าอื่น) บังคับให้คำขอถัดไป
    (รวมถึง poll แรกของ system_update_status.php) คอมไพล์โค้ดใหม่จากดิสก์แน่นอน
  - เพิ่มการอ่านกลับ (`readback`) ไฟล์สถานะทันทีหลังเขียนในคำขอเดียวกัน — ถ้าเขียนสำเร็จ
    แต่อ่านกลับไม่ตรง จะแนบ path จริง + สถานะ written/readback ไว้ใน msg ให้ debug ต่อได้
    ทันทีจาก UI โดยไม่ต้องเข้าเครื่อง รพ. นั้นโดยตรง

## 2026.08.25.2041
- **แก้บั๊กจริง (root cause ยืนยันจาก log): progress bar ไม่ขึ้นระหว่างอัปเดตช้า/ยาวนาน**
  ผู้ใช้แปะ log ไฟล์ `logs/update_<timestamp>.log` เต็มให้ดู ยืนยันว่า `task/update.ps1`
  ทำงานถูกต้องสมบูรณ์ทุกขั้นตอนจริง (การอัปเดตครั้งนั้นใช้เวลา ~3 นาที เพราะดาวน์โหลด ZIP
  จาก GitHub ช้า) — ตัดทฤษฎีเรื่องสิทธิ์เขียนไฟล์ทิ้งไป **สาเหตุจริง: หน้าเว็บเริ่ม poll
  สถานะก็ต่อเมื่อเพิ่งกดปุ่ม "อัปเดตตอนนี้" ในหน้านั้นสด ๆ เท่านั้น** — ถ้ารีเฟรชหน้า/สลับแท็บ/
  แท็บโดน browser throttle ระหว่างรอ (ปกติมากสำหรับงานที่ใช้เวลาหลายนาที) ตัวจับเวลา JS
  จะหายไปและไม่มีอะไร resume ให้เอง แม้ backend จะอัปเดตเสร็จสมบูรณ์แล้วก็ตาม
  - `settings.php`: เพิ่มการเช็คสถานะทันทีตอนโหลดหน้า (`verResumeFromStatus()`) — ถ้ามี
    การอัปเดตกำลังทำงานอยู่เบื้องหลัง (หรือเพิ่งเสร็จ/ล้มเหลวไปไม่เกิน 30 นาที) จะโชว์
    progress bar + resume poll อัตโนมัติทันที ไม่ต้องกดปุ่มซ้ำ ("จับจาก logs หากขึ้น done
    ให้ progress bar วิ่งไปที่ 100%" ตามที่ขอ)
  - เพิ่ม `visibilitychange` listener เช็คสถานะทันทีเมื่อสลับกลับมาที่แท็บระหว่าง poll
  - Refactor: ย้าย logic อัปเดต DOM ตาม step/status ออกมาเป็นฟังก์ชันกลาง `applyStatus()`
    ใช้ร่วมกันทั้ง interval ปกติ, ตอนโหลดหน้า, และตอนกลับมาที่แท็บ (ลด duplicate code)
  - มี freshness guard 30 นาที กันไม่ให้สถานะเก่าค้าง (เช่น update.ps1 โดนฆ่ากลางคัน) โผล่มา
    หลอกผู้ใช้ทุกครั้งที่เข้าหน้านี้ตลอดไป

## 2026.08.25.2022
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อให้ รพ. ที่เพิ่งอัปเดตเป็น 2016 (แก้ไข
  status-write warning + no-store polling + idle-timeout warning) มีเวอร์ชันใหม่
  ให้ทดสอบปุ่ม "อัปเดตตอนนี้" อีกรอบ — ครั้งนี้ถ้าเขียนไฟล์สถานะไม่ได้จะเห็นคำเตือน
  ชัดเจนในข้อความตอบกลับทันที (ไม่เงียบเหมือนก่อนหน้า) ช่วยยืนยัน root cause ได้

## 2026.08.25.2016
- **แก้ไขเพิ่มเติม progress bar สำหรับ รพ. ที่ยัง idle ตลอด:** เจอกรณีที่ `รพ.อื่น`
  ทดสอบแล้ว `system_update_status.php` ตอบ `{"status":"idle"}` ตลอดทุกครั้งที่ poll
  (แปลว่า `logs/update_status.json` ไม่เคยถูกสร้าง/เขียนสำเร็จเลยบนเครื่องนั้น —
  น่าจะเป็นปัญหาสิทธิ์เขียนโฟลเดอร์ `logs/` เฉพาะเครื่อง ต่างจากเครื่องทดสอบของทีม)
  - `system_update_action.php`: ตรวจผลลัพธ์การเขียนไฟล์สถานะจริง (ไม่ปล่อยผ่านเงียบๆ
    แบบเดิม) ถ้าเขียนไม่สำเร็จจะแนบคำเตือนในข้อความตอบกลับทันที ("เขียนไฟล์สถานะไม่ได้ —
    โฟลเดอร์ logs/ อาจไม่มีสิทธิ์เขียน") ให้เห็นสาเหตุชัดเจนแทนที่จะเงียบ
  - `settings.php`: เพิ่ม `cache:'no-store'` กับทุก fetch ของการ poll (กันเบราว์เซอร์
    บางตัว cache response GET ซ้ำๆ) และถ้า poll เจอ `idle` ติดกันเกิน 6 ครั้ง (~9 วิ)
    จะโชว์ข้อความเตือนสีเหลืองอธิบายสาเหตุที่เป็นไปได้ แทนที่จะปล่อยแท่ง progress ค้าง
    นิ่งเฉยๆ โดยไม่มีคำอธิบายใดๆ

## 2026.08.25.2006
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบปุ่ม "อัปเดตตอนนี้" อีกรอบ
  หลังแก้บั๊ก progress bar ค้าง/ไม่ขยับใน 2026.08.25.1916 (เครื่องที่เพิ่ง
  อัปเดตเป็น 1916 จะได้มีเวอร์ชันใหม่ให้ทดสอบเห็น % วิ่งจริงบนแท่ง progress)

## 2026.08.25.1916
- **แก้บั๊ก: progress bar ตอนอัปเดตดูเหมือนค้าง/ไม่ขยับ** สาเหตุ: `system_update_action.php`
  ตอนกด "อัปเดตตอนนี้" ไม่ได้ล้าง/รีเซ็ต `logs/update_status.json` ก่อนสั่งรัน — ถ้ามีไฟล์
  สถานะเก่าจากรอบก่อนค้างอยู่ (`status:'done'`) หน้าเว็บอาจ poll เจอสถานะเก่าทันทีในไม่กี่
  วินาทีแรก ทำให้ progress bar โดดไป 100% หรือดูนิ่งไม่ขยับ ทั้งที่ของจริงยังทำงานอยู่
  เบื้องหลัง — แก้โดยให้ `action=run` เขียนสถานะ `running` step=0 ทันทีแบบ sync ก่อนสั่ง
  รัน update.bat เสมอ
- ปรับ progress bar ให้ดูมีชีวิตขึ้น: โชว์ตัวเลข % บนแท่ง, poll ถี่ขึ้นจาก 3 วิ → 1.5 วิ,
  และเช็คสถานะทันที (ไม่รอ interval แรก) ตอนเริ่มอัปเดต

## 2026.08.25.1856
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อให้ รพ. อื่นที่ยังอยู่เวอร์ชันเก่ากว่า
  ทดสอบปุ่ม "อัปเดตตอนนี้" แบบเต็มรูปแบบได้ (เห็น progress bar 5 ขั้นตอน +
  ปุ่ม "Update Success" ที่เพิ่งเพิ่มใน 2026.08.25.1841 จริงๆ) — เครื่องที่อยู่
  1841 อยู่แล้วจะไม่เห็น "มีอัปเดต" เพราะเป็นเวอร์ชันล่าสุดไปแล้วก่อนหน้านี้

## 2026.08.25.1841
- **Sidebar**: เปลี่ยนชื่อโปรเจกต์จาก "Fall Risk Alert" เป็น **"MedAlert"** พร้อม
  ห้อยเลขเวอร์ชันปัจจุบันต่อท้าย (เช่น `V.2026.08.25.1841`) — ตรงตามชื่อระบบที่
  `CONTEXT.md` กำหนดไว้อยู่แล้ว (จุดอื่นที่ยังเป็นชื่อเก่าอยู่ เช่น browser
  `<title>`, footer ของ Flex fracture ฯลฯ ยังไม่แตะ — นอกขอบเขตงานนี้)
- **ปุ่มอัปเดต**: เพิ่ม progress bar แสดงความคืบหน้าระหว่างอัปเดต (5 ขั้นตอน:
  เริ่ม/สำรอง → git หรือ ZIP → คัดลอกไฟล์ (เฉพาะ ZIP) → migration DB → เสร็จ)
  แทนที่จะมีแค่ข้อความเปลี่ยนไปเรื่อยๆ — `task/update.ps1` เขียน `step`/
  `totalSteps` ลง `logs/update_status.json` เพิ่ม ถ้า error progress bar จะค้าง
  นิ่งตรงขั้นที่พัง (ไม่ reset เป็น 0) ให้เห็นว่าไปถึงไหนก่อนพัง
- เมื่ออัปเดตเสร็จ (`done`) จะไม่เด้ง SweetAlert อัตโนมัติอีกต่อไป — เปลี่ยนเป็น
  โชว์ปุ่ม **"Update Success"** ให้กดเอง แล้วค่อยเด้ง SweetAlert2 แจ้งผล (ให้
  ผู้ใช้กดยืนยันว่าเห็นผลอัปเดตแล้วจริงๆ แทนที่จะเป็น popup แทรกโดยไม่ทันตั้งตัว)

## 2026.08.25.1749
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบปุ่ม "อัปเดตตอนนี้" แบบเต็มรูปแบบ
  ตามคำขอของผู้ใช้ (ยืนยันว่า check→confirm→run→poll สถานะทำงานจบครบวงจรจริง)

## 2026.08.25.1258
- เพิ่ม Feature ใหม่: **แจ้งเตือน "อัปเดตระบบสำเร็จ"** ผ่าน LINE (MOPH Alert) +
  Telegram อัตโนมัติ ทุกครั้งที่กด "อัปเดตตอนนี้" แล้วเสร็จสมบูรณ์ — ข้อความ
  บอกเวอร์ชันใหม่ + เวลาอัปเดต + ชื่อ รพ. (ไม่ใส่รายละเอียด CHANGELOG เพื่อให้
  สั้น อ่านง่าย)
- เพิ่มการ์ด **"แจ้งเตือนอัปเดตระบบ"** ในหน้า `moph_keys_admin.php` ให้ตั้งค่า
  client/secret key + Telegram chat ID เฉพาะสำหรับแจ้งเตือนนี้ได้ (แยกจาก
  Default และโมดูลอื่น เหมือน pattern เดิมของทุก module) — ถ้าไม่ตั้งจะ
  fallback ไปใช้ Default key เหมือนโมดูลอื่นทุกประการ
- `task/update.ps1` เรียก `send_update_notification.php` เป็นขั้นตอนสุดท้าย
  หลังอัปเดตโค้ด+migrate DB เสร็จ — best-effort เสมอ (แจ้งเตือนพลาดไม่ทำให้
  ผลอัปเดตกลายเป็น error)

## 2026.08.25.1231
- **แก้บั๊กร้ายแรง: COVID-19 ไม่เคยใช้คีย์ MOPH เฉพาะ module เลย** — ตั้งค่า
  client/secret key ของ COVID ที่หน้า "MOPH ALERT KEY" แล้วบันทึกสำเร็จ แต่
  แจ้งเตือนจริง (ทั้ง worker อัตโนมัติและปุ่ม "ส่งซ้ำทันที") ยังใช้คีย์ Default
  เสมอ — `covid_lib.php` อ้างอิง `MOPH_CLIENT_KEY`/`MOPH_SECRET_KEY` (ค่า
  default) ตรงๆ แทนที่จะเป็น `COVID_CLIENT_KEY`/`COVID_SECRET_KEY` เหมือนอีก
  9 module (เข้าใจว่าโค้ดนี้เก่ากว่าระบบ per-module key ทั้งหมด และไม่เคยถูก
  แก้ตาม) ทดสอบยืนยันแล้วว่าตอนนี้ resolve คีย์เฉพาะ COVID ถูกต้อง +
  ยัง fallback ไป default ถูกต้องเมื่อไม่ได้ตั้งคีย์เฉพาะไว้
- เพิ่ม Telegram mirror ให้ COVID-19 (เดิมไม่มีเลย ต่างจากอีก 7 module —
  ตั้งค่า Telegram chat_id เฉพาะ COVID ไว้ก็ไม่มีผลอะไรมาก่อนหน้านี้)

## 2026.08.24.1620
- **แก้บั๊กร้ายแรง: กดปุ่ม "อัปเดตตอนนี้" แล้วค้างที่สถานะ idle ตลอด ไม่ทำงานจริง**
  สาเหตุ 2 อย่างซ้อนกัน (ผู้ใช้ทดสอบแล้วเจอเอง):
  1. `task/update.bat` มีโค้ด self-elevate (ขอสิทธิ์ Administrator ผ่าน UAC) —
     พอปุ่มเว็บสั่งรันแบบ background ไม่มีใครเห็น/กด UAC prompt ได้ สคริปต์เลย
     ค้างรอตลอดไป ทั้งที่งานที่ทำ (copy ไฟล์ในโฟลเดอร์ตัวเอง, git, เรียก php)
     ไม่ต้องใช้สิทธิ์ Administrator เลยตั้งแต่แรก — เอา self-elevate ออก
  2. แม้เอา UAC ออกแล้ว `proc_open()` ของ PHP บน Windows ยังคง "ฆ่า" child
     process ทันทีที่ request จบ (ผูกกับ Job Object) ทำให้ update.bat ไม่เคย
     รันจริงอยู่ดี — เปลี่ยนไปใช้ `wscript.exe //B` + `WScript.Shell.Run(...,
     False)` แทน (ทดสอบยืนยันแล้วว่า detach จริง รอดจาก lifecycle ของ PHP)
- เพิ่ม `task/launch_detached.vbs` (launcher ที่ทำข้อ 2)

## 2026.08.24.1554
- แก้ VERSION ที่ค้างจาก `2026.08.24` (Phase A) มา 7 commit โดยไม่เคยขยับ —
  ทำให้ปุ่ม "ตรวจสอบอัปเดต" ใช้งานไม่ได้จริงมาตลอดตั้งแต่ระบบนี้เริ่มใช้
- เปลี่ยนรูปแบบเวอร์ชันเป็น `YYYY.MM.DD.HHMM` กันปัญหาเดิมไม่ให้เกิดซ้ำ
- เพิ่มหน้า "เกี่ยวกับระบบ" (`about.php`) ในเมนู "ตั้งค่าระบบ" — รวมคู่มือติดตั้ง
  และคู่มืออัปเดตไว้ในแอปเป็น 2 แท็บ ไม่ต้องออกไปดูไฟล์แยก
- เพิ่ม `docs/update-guide.html` (คู่มืออัปเดตแยกจากคู่มือติดตั้ง) +
  ทางเลือก PowerShell สำหรับดาวน์โหลดสคริปต์ตอน bootstrap เวอร์ชันเก่า
- แก้โน้ตเก่าใน `docs/install-guide.html` ที่บอกให้ import lepto/scrub เอง
  ทั้งที่ตอนนี้ auto แล้ว

## 2026.08.24
- เพิ่มระบบอัปเดตเวอร์ชัน: ปุ่ม "ตรวจสอบ/อัปเดต" ในหน้า `settings.php`, สคริปต์ `task/update.bat` (รองรับทั้ง git และ ZIP), สำรองข้อมูลอัตโนมัติก่อนอัปเดตทุกครั้ง
- แก้ปุ่ม "Setup MedAlert_DB" ให้ deploy schema แบบ glob (`*_queue.sql`) แทน list ตายตัว — ทำให้ `lepto_queue`/`scrub_queue` ที่เคยตกหล่นถูกสร้างอัตโนมัติด้วย
- ระบบ "แก้ไขเงื่อนไขดึงข้อมูล" ครบทั้ง 10 module (covid, accident, drug, sexual, lepto, scrub, dengue, patient, fracture, pharm_lab) — แก้รหัส lab/pttype/icode/pdx/เกณฑ์ผ่านหน้าเว็บได้เอง ไม่ต้องแก้โค้ด
