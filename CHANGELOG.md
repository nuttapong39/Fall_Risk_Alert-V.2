# Changelog

รูปแบบเวอร์ชัน: `YYYY.MM.DD.HHMM` (วันที่+เวลาปล่อยรีลีส ตามเวลาที่ commit) —
ใช้เวลาต่อท้ายเพราะวันเดียวอาจปล่อยมากกว่า 1 รีลีส (ถ้าใช้แค่ `YYYY.MM.DD`
เฉยๆ รีลีสที่ 2 ของวันจะมีเลขเวอร์ชันชนกับรีลีสแรก ทำให้ปุ่ม "ตรวจสอบอัปเดต"
เข้าใจผิดว่าไม่มีอะไรใหม่)

**สำคัญ:** ทุกครั้งที่ commit อะไรที่มีผลกับ รพ. ที่ deploy ไปแล้ว
(โค้ด/schema/เอกสารที่ผู้ใช้เห็น) ต้องอัปเดตไฟล์ `VERSION` เป็นวันเวลาปัจจุบัน
เสมอ — ไม่งั้นระบบตรวจอัปเดตจะใช้งานไม่ได้ (เคยพลาดมาแล้ว: ปล่อยไป 7 commit
ในวันเดียวโดยไม่แตะ VERSION เลย ทำให้เครื่องที่อัปเดตแล้วกับเครื่องที่ยังไม่ได้
อัปเดตโชว์เลขเวอร์ชันเดียวกัน)

## 2026.08.25.1856
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อให้ รพ. อื่นที่ยังอยู่เวอร์ชันเก่ากว่า
  ทดสอบปุ่ม "อัปเดตตอนนี้" แบบเต็มรูปแบบได้ (เห็น progress bar 5 ขั้นตอน +
  ปุ่ม "Update Success" ที่เพิ่งเพิ่มใน 2026.08.25.1841 จริงๆ) — เครื่องที่อยู่
  1841 อยู่แล้วจะไม่เห็น "มีอัปเดต" เพราะเป็นเวอร์ชันล่าสุดไปแล้วก่อนหน้านี้

## 2026.08.25.1841
- **Sidebar**: เปลี่ยนชื่อโปรเจกต์จาก "Fall Risk Alert" เป็น **"MedAlert"** พร้อม
  ห้อยเลขเวอร์ชันปัจจุบันต่อท้าย (เช่น `V.2026.08.25.1841`) — ตรงตามชื่อระบบที่
  `CONTEXT.md` กำหนดไว้อยู่แล้ว (จุดอื่นที่ยังเป็นชื่อเก่าอยู่ เช่น browser
  `<title>`, footer ของ Flex fracture ฯลฯ ยังไม่แตะ — นอกขอบเขตงานนี้)
- **ปุ่มอัปเดต**: เพิ่ม progress bar แสดงความคืบหน้าระหว่างอัปเดต (5 ขั้นตอน:
  เริ่ม/สำรอง → git หรือ ZIP → คัดลอกไฟล์ (เฉพาะ ZIP) → migration DB → เสร็จ)
  แทนที่จะมีแค่ข้อความเปลี่ยนไปเรื่อยๆ — `task/update.ps1` เขียน `step`/
  `totalSteps` ลง `logs/update_status.json` เพิ่ม ถ้า error progress bar จะค้าง
  นิ่งตรงขั้นที่พัง (ไม่ reset เป็น 0) ให้เห็นว่าไปถึงไหนก่อนพัง
- เมื่ออัปเดตเสร็จ (`done`) จะไม่เด้ง SweetAlert อัตโนมัติอีกต่อไป — เปลี่ยนเป็น
  โชว์ปุ่ม **"Update Success"** ให้กดเอง แล้วค่อยเด้ง SweetAlert2 แจ้งผล (ให้
  ผู้ใช้กดยืนยันว่าเห็นผลอัปเดตแล้วจริงๆ แทนที่จะเป็น popup แทรกโดยไม่ทันตั้งตัว)

## 2026.08.25.1749
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบปุ่ม "อัปเดตตอนนี้" แบบเต็มรูปแบบ
  ตามคำขอของผู้ใช้ (ยืนยันว่า check→confirm→run→poll สถานะทำงานจบครบวงจรจริง)

## 2026.08.25.1258
- เพิ่ม Feature ใหม่: **แจ้งเตือน "อัปเดตระบบสำเร็จ"** ผ่าน LINE (MOPH Alert) +
  Telegram อัตโนมัติ ทุกครั้งที่กด "อัปเดตตอนนี้" แล้วเสร็จสมบูรณ์ — ข้อความ
  บอกเวอร์ชันใหม่ + เวลาอัปเดต + ชื่อ รพ. (ไม่ใส่รายละเอียด CHANGELOG เพื่อให้
  สั้น อ่านง่าย)
- เพิ่มการ์ด **"แจ้งเตือนอัปเดตระบบ"** ในหน้า `moph_keys_admin.php` ให้ตั้งค่า
  client/secret key + Telegram chat ID เฉพาะสำหรับแจ้งเตือนนี้ได้ (แยกจาก
  Default และโมดูลอื่น เหมือน pattern เดิมของทุก module) — ถ้าไม่ตั้งจะ
  fallback ไปใช้ Default key เหมือนโมดูลอื่นทุกประการ
- `task/update.ps1` เรียก `send_update_notification.php` เป็นขั้นตอนสุดท้าย
  หลังอัปเดตโค้ด+migrate DB เสร็จ — best-effort เสมอ (แจ้งเตือนพลาดไม่ทำให้
  ผลอัปเดตกลายเป็น error)

## 2026.08.25.1231
- **แก้บั๊กร้ายแรง: COVID-19 ไม่เคยใช้คีย์ MOPH เฉพาะ module เลย** — ตั้งค่า
  client/secret key ของ COVID ที่หน้า "MOPH ALERT KEY" แล้วบันทึกสำเร็จ แต่
  แจ้งเตือนจริง (ทั้ง worker อัตโนมัติและปุ่ม "ส่งซ้ำทันที") ยังใช้คีย์ Default
  เสมอ — `covid_lib.php` อ้างอิง `MOPH_CLIENT_KEY`/`MOPH_SECRET_KEY` (ค่า
  default) ตรงๆ แทนที่จะเป็น `COVID_CLIENT_KEY`/`COVID_SECRET_KEY` เหมือนอีก
  9 module (เข้าใจว่าโค้ดนี้เก่ากว่าระบบ per-module key ทั้งหมด และไม่เคยถูก
  แก้ตาม) ทดสอบยืนยันแล้วว่าตอนนี้ resolve คีย์เฉพาะ COVID ถูกต้อง +
  ยัง fallback ไป default ถูกต้องเมื่อไม่ได้ตั้งคีย์เฉพาะไว้
- เพิ่ม Telegram mirror ให้ COVID-19 (เดิมไม่มีเลย ต่างจากอีก 7 module —
  ตั้งค่า Telegram chat_id เฉพาะ COVID ไว้ก็ไม่มีผลอะไรมาก่อนหน้านี้)

## 2026.08.24.1620
- **แก้บั๊กร้ายแรง: กดปุ่ม "อัปเดตตอนนี้" แล้วค้างที่สถานะ idle ตลอด ไม่ทำงานจริง**
  สาเหตุ 2 อย่างซ้อนกัน (ผู้ใช้ทดสอบแล้วเจอเอง):
  1. `task/update.bat` มีโค้ด self-elevate (ขอสิทธิ์ Administrator ผ่าน UAC) —
     พอปุ่มเว็บสั่งรันแบบ background ไม่มีใครเห็น/กด UAC prompt ได้ สคริปต์เลย
     ค้างรอตลอดไป ทั้งที่งานที่ทำ (copy ไฟล์ในโฟลเดอร์ตัวเอง, git, เรียก php)
     ไม่ต้องใช้สิทธิ์ Administrator เลยตั้งแต่แรก — เอา self-elevate ออก
  2. แม้เอา UAC ออกแล้ว `proc_open()` ของ PHP บน Windows ยังคง "ฆ่า" child
     process ทันทีที่ request จบ (ผูกกับ Job Object) ทำให้ update.bat ไม่เคย
     รันจริงอยู่ดี — เปลี่ยนไปใช้ `wscript.exe //B` + `WScript.Shell.Run(...,
     False)` แทน (ทดสอบยืนยันแล้วว่า detach จริง รอดจาก lifecycle ของ PHP)
- เพิ่ม `task/launch_detached.vbs` (launcher ที่ทำข้อ 2)

## 2026.08.24.1554
- แก้ VERSION ที่ค้างจาก `2026.08.24` (Phase A) มา 7 commit โดยไม่เคยขยับ —
  ทำให้ปุ่ม "ตรวจสอบอัปเดต" ใช้งานไม่ได้จริงมาตลอดตั้งแต่ระบบนี้เริ่มใช้
- เปลี่ยนรูปแบบเวอร์ชันเป็น `YYYY.MM.DD.HHMM` กันปัญหาเดิมไม่ให้เกิดซ้ำ
- เพิ่มหน้า "เกี่ยวกับระบบ" (`about.php`) ในเมนู "ตั้งค่าระบบ" — รวมคู่มือติดตั้ง
  และคู่มืออัปเดตไว้ในแอปเป็น 2 แท็บ ไม่ต้องออกไปดูไฟล์แยก
- เพิ่ม `docs/update-guide.html` (คู่มืออัปเดตแยกจากคู่มือติดตั้ง) +
  ทางเลือก PowerShell สำหรับดาวน์โหลดสคริปต์ตอน bootstrap เวอร์ชันเก่า
- แก้โน้ตเก่าใน `docs/install-guide.html` ที่บอกให้ import lepto/scrub เอง
  ทั้งที่ตอนนี้ auto แล้ว

## 2026.08.24
- เพิ่มระบบอัปเดตเวอร์ชัน: ปุ่ม "ตรวจสอบ/อัปเดต" ในหน้า `settings.php`, สคริปต์ `task/update.bat` (รองรับทั้ง git และ ZIP), สำรองข้อมูลอัตโนมัติก่อนอัปเดตทุกครั้ง
- แก้ปุ่ม "Setup MedAlert_DB" ให้ deploy schema แบบ glob (`*_queue.sql`) แทน list ตายตัว — ทำให้ `lepto_queue`/`scrub_queue` ที่เคยตกหล่นถูกสร้างอัตโนมัติด้วย
- ระบบ "แก้ไขเงื่อนไขดึงข้อมูล" ครบทั้ง 10 module (covid, accident, drug, sexual, lepto, scrub, dengue, patient, fracture, pharm_lab) — แก้รหัส lab/pttype/icode/pdx/เกณฑ์ผ่านหน้าเว็บได้เอง ไม่ต้องแก้โค้ด
