# Changelog

รูปแบบเวอร์ชัน: `YYYY.MM.DD.HHMM` (วันที่+เวลาปล่อยรีลีส ตามเวลาที่ commit) —
ใช้เวลาต่อท้ายเพราะวันเดียวอาจปล่อยมากกว่า 1 รีลีส (ถ้าใช้แค่ `YYYY.MM.DD`
เฉยๆ รีลีสที่ 2 ของวันจะมีเลขเวอร์ชันชนกับรีลีสแรก ทำให้ปุ่ม "ตรวจสอบอัปเดต"
เข้าใจผิดว่าไม่มีอะไรใหม่)

**สำคัญ:** ทุกครั้งที่ commit อะไรที่มีผลกับ รพ. ที่ deploy ไปแล้ว
(โค้ด/schema/เอกสารที่ผู้ใช้เห็น) ต้องอัปเดตไฟล์ `VERSION` เป็นวันเวลาปัจจุบัน
เสมอ — ไม่งั้นระบบตรวจอัปเดตจะใช้งานไม่ได้ (เคยพลาดมาแล้ว: ปล่อยไป 7 commit
ในวันเดียวโดยไม่แตะ VERSION เลย ทำให้เครื่องที่อัปเดตแล้วกับเครื่องที่ยังไม่ได้
อัปเดตโชว์เลขเวอร์ชันเดียวกัน)

## 2026.08.25.2202
- **แก้บั๊กร้ายแรง: อัปเดตค้างเงียบๆ ไม่ error ไม่ progress ต่อ (ตอนขั้นตอนสำรองข้อมูล)**
  ทั้ง 2 จุดที่เรียก `robocopy` ใน `task/update.ps1` ไม่เคยระบุ `/R` (จำนวนครั้ง retry)
  กับ `/W` (เวลารอระหว่าง retry) มาก่อน — ค่า default ของ robocopy คือ **retry 1,000,000
  ครั้ง รอครั้งละ 30 วินาที** ถ้าเจอไฟล์แม้แค่ไฟล์เดียวที่ถูกล็อกอยู่ (เช่น antivirus กำลังสแกน,
  หรือไฟล์ log ที่ script นี้เองกำลังเขียนอยู่พอดี) robocopy จะค้างรอเงียบๆ แบบดูเหมือนแฮง
  ตลอดกาล ไม่ error ไม่ progress ต่อ — ยืนยันสาเหตุด้วยการจำลองไฟล์ล็อกจริงบนเครื่อง dev
  (ยืนยันว่าไม่ระบุ `/R /W` ทำให้ค้าง, พอใส่ `/R:2 /W:2` แล้วล้มเหลวไว ~4 วิ แทน)
  - เพิ่ม `/R:2 /W:2` ให้ทั้ง 2 จุด (สำรองข้อมูล + คัดลอกไฟล์ทับ path ZIP) — ถ้าไฟล์ยังล็อก
    อยู่หลัง retry 2 ครั้ง จะ throw error ให้เห็นชัดเจนแทนที่จะค้างไม่มีกำหนด

## 2026.08.25.2130
- **แก้บั๊กใหม่: อัปเดตล้มเหลวแบบสุ่ม `Cannot find path '...\.gitignore' because it does not exist`**
  ระหว่างขั้นตอนคัดลอกไฟล์ (path ZIP) — สาเหตุที่เป็นไปได้มากที่สุดคือกดปุ่ม "อัปเดตตอนนี้"
  ซ้อนกัน 2 ครั้ง (เช่น รีเฟรชหน้าแล้วกดซ้ำระหว่างที่รอบแรกยังทำ ZIP download อยู่ ซึ่งอาจ
  ใช้เวลาหลายนาที) ทำให้ 2 โปรเซส `update.ps1` แย่งกันอ่าน/เขียนไฟล์ temp เดียวกัน — ไม่มี
  กลไกกันการสั่งซ้อนมาก่อนเลย
  - `system_update_action.php`: เพิ่ม concurrent-run guard — ถ้ามีสถานะ `running` สดๆ
    (ไม่เกิน 10 นาที) อยู่แล้ว จะปฏิเสธคำขออัปเดตใหม่ทันทีพร้อมข้อความแจ้งให้รอ แทนที่จะ
    ปล่อยให้ 2 โปรเซสชนกัน (เกิน 10 นาทีถือว่าสถานะค้าง ไม่ block ของจริงตลอดไป)

## 2026.08.25.2123
- อัปเดต `docs/update-guide.html` ให้ตรงกับฟีเจอร์ล่าสุด — เพิ่ม mockup progress bar
  + ปุ่ม "Update Success" (แทน mockup log ดิบอย่างเดียว), เพิ่มโน้ตเรื่องแจ้งเตือน
  LINE/Telegram อัตโนมัติหลังอัปเดตสำเร็จ, เพิ่มโน้ตว่ารีเฟรช/ปิดแท็บระหว่างรอได้ไม่มี
  ปัญหา (ระบบ resume สถานะให้อัตโนมัติ), เพิ่ม troubleshooting เรื่องแถบค้างที่ 90-95%,
  ปรับตัวอย่างเลขเวอร์ชันเป็นรูปแบบ `YYYY.MM.DD.HHMM` ให้ตรงปัจจุบัน (จากเดิม
  `YYYY.MM.DD` เก่า), เพิ่ม checklist ข้อตรวจสอบแจ้งเตือน LINE/Telegram

## 2026.08.25.2116
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบ hybrid time-based progress bar
  (2107) ให้ถูกต้อง **สำคัญ: ต้องกด F5 รีเฟรชหน้า settings.php ก่อนกดอัปเดต**
  เพราะรอบก่อนหน้าเห็นข้อความเตือนเวอร์ชันเก่า แปลว่าเบราว์เซอร์ยังโหลด JS ค้างจาก
  ก่อนอัปเดต ไม่ใช่ตัวที่เพิ่งดึงมาไว้ในเครื่อง

## 2026.08.25.2107
- **เพิ่ม progress bar แบบ hybrid — การันตีเห็นความเคลื่อนไหวเสมอ ไม่ว่าสถานะจริงจาก
  server จะอ่านได้หรือไม่:** ยังไม่ยืนยัน root cause 100% ว่า opcache (2054) แก้ปัญหา
  progress bar idle ตลอดของบาง รพ. ได้จริงหรือไม่ — เพิ่มชั้นป้องกันเพิ่มเติมที่ไม่ต้องพึ่ง
  การแก้บั๊กต้นตอให้เสร็จก่อน: คำนวณ % จำลองจากเวลาที่ผ่านไปจริงตั้งแต่กดปุ่ม (ประมาณ 4 นาที
  = 100%, เพดานที่ 95% ไม่ยืนยันว่าเสร็จจริงจนกว่าจะเจอสถานะ done จริง) แล้วโชว์ค่าที่มากกว่า
  ระหว่าง % จริงจาก server กับ % จำลอง — ถ้าสถานะจริงอ่านได้ (ปกติ) จะแม่นยำตามเดิม ถ้าอ่านไม่ได้
  (เครื่องที่มีปัญหาเฉพาะจุด) แถบจะยังขยับตามเวลาให้เห็นเสมอ ไม่ค้างว่างเปล่าอีกต่อไป
  ปรับข้อความเตือน idle ให้บอกตรงๆ ว่า "แถบกำลังประมาณจากเวลาที่ผ่านไปแทน"

## 2026.08.25.2054
- **สงสัยสาเหตุใหม่ของบั๊ก "progress bar ไม่ขึ้น" ที่เกิดซ้ำแม้แก้ frontend แล้ว (2041):**
  รพ. อื่นทดสอบอัปเดตไปที่ 2041 แล้วยัง idle-warning เหมือนเดิม ทั้งที่ log ยืนยันว่า
  `update.ps1` ทำงานถูกต้อง 100% เหมือนทุกครั้ง — สังเกตว่าอาการเดิมเป๊ะเกิดซ้ำข้ามหลาย
  commit ที่ genuinely แก้โค้ดคนละจุดกัน (ครั้งนี้คือ resume-polling ฝั่ง JS ไม่เกี่ยวกับ
  การเขียนไฟล์สถานะเลยด้วยซ้ำ) ลักษณะแบบนี้เข้าข่าย **PHP OPcache รันโค้ดเก่าค้าง**
  (ไฟล์บนดิสก์ถูกอัปเดตทับจริง แต่ bytecode ที่ compile ไว้ในหน่วยความจำยังเป็นเวอร์ชันเก่า
  ถ้าเซิร์ฟเวอร์ตั้ง `opcache.validate_timestamps=0` เพื่อ perf) — อธิบายได้ว่าทำไมแก้
  ถูกจุดแล้วแต่ runtime behavior เหมือนเดิมไม่เปลี่ยน
  - `system_update_action.php`: เพิ่ม `opcache_reset()` ทันทีที่ action=check/run ถูกเรียก
    (ผู้ใช้กดเอง ไม่ใช่ polling ที่ถี่ทุก 1.5 วิ กัน perf กระทบหน้าอื่น) บังคับให้คำขอถัดไป
    (รวมถึง poll แรกของ system_update_status.php) คอมไพล์โค้ดใหม่จากดิสก์แน่นอน
  - เพิ่มการอ่านกลับ (`readback`) ไฟล์สถานะทันทีหลังเขียนในคำขอเดียวกัน — ถ้าเขียนสำเร็จ
    แต่อ่านกลับไม่ตรง จะแนบ path จริง + สถานะ written/readback ไว้ใน msg ให้ debug ต่อได้
    ทันทีจาก UI โดยไม่ต้องเข้าเครื่อง รพ. นั้นโดยตรง

## 2026.08.25.2041
- **แก้บั๊กจริง (root cause ยืนยันจาก log): progress bar ไม่ขึ้นระหว่างอัปเดตช้า/ยาวนาน**
  ผู้ใช้แปะ log ไฟล์ `logs/update_<timestamp>.log` เต็มให้ดู ยืนยันว่า `task/update.ps1`
  ทำงานถูกต้องสมบูรณ์ทุกขั้นตอนจริง (การอัปเดตครั้งนั้นใช้เวลา ~3 นาที เพราะดาวน์โหลด ZIP
  จาก GitHub ช้า) — ตัดทฤษฎีเรื่องสิทธิ์เขียนไฟล์ทิ้งไป **สาเหตุจริง: หน้าเว็บเริ่ม poll
  สถานะก็ต่อเมื่อเพิ่งกดปุ่ม "อัปเดตตอนนี้" ในหน้านั้นสด ๆ เท่านั้น** — ถ้ารีเฟรชหน้า/สลับแท็บ/
  แท็บโดน browser throttle ระหว่างรอ (ปกติมากสำหรับงานที่ใช้เวลาหลายนาที) ตัวจับเวลา JS
  จะหายไปและไม่มีอะไร resume ให้เอง แม้ backend จะอัปเดตเสร็จสมบูรณ์แล้วก็ตาม
  - `settings.php`: เพิ่มการเช็คสถานะทันทีตอนโหลดหน้า (`verResumeFromStatus()`) — ถ้ามี
    การอัปเดตกำลังทำงานอยู่เบื้องหลัง (หรือเพิ่งเสร็จ/ล้มเหลวไปไม่เกิน 30 นาที) จะโชว์
    progress bar + resume poll อัตโนมัติทันที ไม่ต้องกดปุ่มซ้ำ ("จับจาก logs หากขึ้น done
    ให้ progress bar วิ่งไปที่ 100%" ตามที่ขอ)
  - เพิ่ม `visibilitychange` listener เช็คสถานะทันทีเมื่อสลับกลับมาที่แท็บระหว่าง poll
  - Refactor: ย้าย logic อัปเดต DOM ตาม step/status ออกมาเป็นฟังก์ชันกลาง `applyStatus()`
    ใช้ร่วมกันทั้ง interval ปกติ, ตอนโหลดหน้า, และตอนกลับมาที่แท็บ (ลด duplicate code)
  - มี freshness guard 30 นาที กันไม่ให้สถานะเก่าค้าง (เช่น update.ps1 โดนฆ่ากลางคัน) โผล่มา
    หลอกผู้ใช้ทุกครั้งที่เข้าหน้านี้ตลอดไป

## 2026.08.25.2022
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อให้ รพ. ที่เพิ่งอัปเดตเป็น 2016 (แก้ไข
  status-write warning + no-store polling + idle-timeout warning) มีเวอร์ชันใหม่
  ให้ทดสอบปุ่ม "อัปเดตตอนนี้" อีกรอบ — ครั้งนี้ถ้าเขียนไฟล์สถานะไม่ได้จะเห็นคำเตือน
  ชัดเจนในข้อความตอบกลับทันที (ไม่เงียบเหมือนก่อนหน้า) ช่วยยืนยัน root cause ได้

## 2026.08.25.2016
- **แก้ไขเพิ่มเติม progress bar สำหรับ รพ. ที่ยัง idle ตลอด:** เจอกรณีที่ `รพ.อื่น`
  ทดสอบแล้ว `system_update_status.php` ตอบ `{"status":"idle"}` ตลอดทุกครั้งที่ poll
  (แปลว่า `logs/update_status.json` ไม่เคยถูกสร้าง/เขียนสำเร็จเลยบนเครื่องนั้น —
  น่าจะเป็นปัญหาสิทธิ์เขียนโฟลเดอร์ `logs/` เฉพาะเครื่อง ต่างจากเครื่องทดสอบของทีม)
  - `system_update_action.php`: ตรวจผลลัพธ์การเขียนไฟล์สถานะจริง (ไม่ปล่อยผ่านเงียบๆ
    แบบเดิม) ถ้าเขียนไม่สำเร็จจะแนบคำเตือนในข้อความตอบกลับทันที ("เขียนไฟล์สถานะไม่ได้ —
    โฟลเดอร์ logs/ อาจไม่มีสิทธิ์เขียน") ให้เห็นสาเหตุชัดเจนแทนที่จะเงียบ
  - `settings.php`: เพิ่ม `cache:'no-store'` กับทุก fetch ของการ poll (กันเบราว์เซอร์
    บางตัว cache response GET ซ้ำๆ) และถ้า poll เจอ `idle` ติดกันเกิน 6 ครั้ง (~9 วิ)
    จะโชว์ข้อความเตือนสีเหลืองอธิบายสาเหตุที่เป็นไปได้ แทนที่จะปล่อยแท่ง progress ค้าง
    นิ่งเฉยๆ โดยไม่มีคำอธิบายใดๆ

## 2026.08.25.2006
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบปุ่ม "อัปเดตตอนนี้" อีกรอบ
  หลังแก้บั๊ก progress bar ค้าง/ไม่ขยับใน 2026.08.25.1916 (เครื่องที่เพิ่ง
  อัปเดตเป็น 1916 จะได้มีเวอร์ชันใหม่ให้ทดสอบเห็น % วิ่งจริงบนแท่ง progress)

## 2026.08.25.1916
- **แก้บั๊ก: progress bar ตอนอัปเดตดูเหมือนค้าง/ไม่ขยับ** สาเหตุ: `system_update_action.php`
  ตอนกด "อัปเดตตอนนี้" ไม่ได้ล้าง/รีเซ็ต `logs/update_status.json` ก่อนสั่งรัน — ถ้ามีไฟล์
  สถานะเก่าจากรอบก่อนค้างอยู่ (`status:'done'`) หน้าเว็บอาจ poll เจอสถานะเก่าทันทีในไม่กี่
  วินาทีแรก ทำให้ progress bar โดดไป 100% หรือดูนิ่งไม่ขยับ ทั้งที่ของจริงยังทำงานอยู่
  เบื้องหลัง — แก้โดยให้ `action=run` เขียนสถานะ `running` step=0 ทันทีแบบ sync ก่อนสั่ง
  รัน update.bat เสมอ
- ปรับ progress bar ให้ดูมีชีวิตขึ้น: โชว์ตัวเลข % บนแท่ง, poll ถี่ขึ้นจาก 3 วิ → 1.5 วิ,
  และเช็คสถานะทันที (ไม่รอ interval แรก) ตอนเริ่มอัปเดต

## 2026.08.25.1856
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อให้ รพ. อื่นที่ยังอยู่เวอร์ชันเก่ากว่า
  ทดสอบปุ่ม "อัปเดตตอนนี้" แบบเต็มรูปแบบได้ (เห็น progress bar 5 ขั้นตอน +
  ปุ่ม "Update Success" ที่เพิ่งเพิ่มใน 2026.08.25.1841 จริงๆ) — เครื่องที่อยู่
  1841 อยู่แล้วจะไม่เห็น "มีอัปเดต" เพราะเป็นเวอร์ชันล่าสุดไปแล้วก่อนหน้านี้

## 2026.08.25.1841
- **Sidebar**: เปลี่ยนชื่อโปรเจกต์จาก "Fall Risk Alert" เป็น **"MedAlert"** พร้อม
  ห้อยเลขเวอร์ชันปัจจุบันต่อท้าย (เช่น `V.2026.08.25.1841`) — ตรงตามชื่อระบบที่
  `CONTEXT.md` กำหนดไว้อยู่แล้ว (จุดอื่นที่ยังเป็นชื่อเก่าอยู่ เช่น browser
  `<title>`, footer ของ Flex fracture ฯลฯ ยังไม่แตะ — นอกขอบเขตงานนี้)
- **ปุ่มอัปเดต**: เพิ่ม progress bar แสดงความคืบหน้าระหว่างอัปเดต (5 ขั้นตอน:
  เริ่ม/สำรอง → git หรือ ZIP → คัดลอกไฟล์ (เฉพาะ ZIP) → migration DB → เสร็จ)
  แทนที่จะมีแค่ข้อความเปลี่ยนไปเรื่อยๆ — `task/update.ps1` เขียน `step`/
  `totalSteps` ลง `logs/update_status.json` เพิ่ม ถ้า error progress bar จะค้าง
  นิ่งตรงขั้นที่พัง (ไม่ reset เป็น 0) ให้เห็นว่าไปถึงไหนก่อนพัง
- เมื่ออัปเดตเสร็จ (`done`) จะไม่เด้ง SweetAlert อัตโนมัติอีกต่อไป — เปลี่ยนเป็น
  โชว์ปุ่ม **"Update Success"** ให้กดเอง แล้วค่อยเด้ง SweetAlert2 แจ้งผล (ให้
  ผู้ใช้กดยืนยันว่าเห็นผลอัปเดตแล้วจริงๆ แทนที่จะเป็น popup แทรกโดยไม่ทันตั้งตัว)

## 2026.08.25.1749
- ไม่มีการเปลี่ยนโค้ด — bump เวอร์ชันเพื่อทดสอบปุ่ม "อัปเดตตอนนี้" แบบเต็มรูปแบบ
  ตามคำขอของผู้ใช้ (ยืนยันว่า check→confirm→run→poll สถานะทำงานจบครบวงจรจริง)

## 2026.08.25.1258
- เพิ่ม Feature ใหม่: **แจ้งเตือน "อัปเดตระบบสำเร็จ"** ผ่าน LINE (MOPH Alert) +
  Telegram อัตโนมัติ ทุกครั้งที่กด "อัปเดตตอนนี้" แล้วเสร็จสมบูรณ์ — ข้อความ
  บอกเวอร์ชันใหม่ + เวลาอัปเดต + ชื่อ รพ. (ไม่ใส่รายละเอียด CHANGELOG เพื่อให้
  สั้น อ่านง่าย)
- เพิ่มการ์ด **"แจ้งเตือนอัปเดตระบบ"** ในหน้า `moph_keys_admin.php` ให้ตั้งค่า
  client/secret key + Telegram chat ID เฉพาะสำหรับแจ้งเตือนนี้ได้ (แยกจาก
  Default และโมดูลอื่น เหมือน pattern เดิมของทุก module) — ถ้าไม่ตั้งจะ
  fallback ไปใช้ Default key เหมือนโมดูลอื่นทุกประการ
- `task/update.ps1` เรียก `send_update_notification.php` เป็นขั้นตอนสุดท้าย
  หลังอัปเดตโค้ด+migrate DB เสร็จ — best-effort เสมอ (แจ้งเตือนพลาดไม่ทำให้
  ผลอัปเดตกลายเป็น error)

## 2026.08.25.1231
- **แก้บั๊กร้ายแรง: COVID-19 ไม่เคยใช้คีย์ MOPH เฉพาะ module เลย** — ตั้งค่า
  client/secret key ของ COVID ที่หน้า "MOPH ALERT KEY" แล้วบันทึกสำเร็จ แต่
  แจ้งเตือนจริง (ทั้ง worker อัตโนมัติและปุ่ม "ส่งซ้ำทันที") ยังใช้คีย์ Default
  เสมอ — `covid_lib.php` อ้างอิง `MOPH_CLIENT_KEY`/`MOPH_SECRET_KEY` (ค่า
  default) ตรงๆ แทนที่จะเป็น `COVID_CLIENT_KEY`/`COVID_SECRET_KEY` เหมือนอีก
  9 module (เข้าใจว่าโค้ดนี้เก่ากว่าระบบ per-module key ทั้งหมด และไม่เคยถูก
  แก้ตาม) ทดสอบยืนยันแล้วว่าตอนนี้ resolve คีย์เฉพาะ COVID ถูกต้อง +
  ยัง fallback ไป default ถูกต้องเมื่อไม่ได้ตั้งคีย์เฉพาะไว้
- เพิ่ม Telegram mirror ให้ COVID-19 (เดิมไม่มีเลย ต่างจากอีก 7 module —
  ตั้งค่า Telegram chat_id เฉพาะ COVID ไว้ก็ไม่มีผลอะไรมาก่อนหน้านี้)

## 2026.08.24.1620
- **แก้บั๊กร้ายแรง: กดปุ่ม "อัปเดตตอนนี้" แล้วค้างที่สถานะ idle ตลอด ไม่ทำงานจริง**
  สาเหตุ 2 อย่างซ้อนกัน (ผู้ใช้ทดสอบแล้วเจอเอง):
  1. `task/update.bat` มีโค้ด self-elevate (ขอสิทธิ์ Administrator ผ่าน UAC) —
     พอปุ่มเว็บสั่งรันแบบ background ไม่มีใครเห็น/กด UAC prompt ได้ สคริปต์เลย
     ค้างรอตลอดไป ทั้งที่งานที่ทำ (copy ไฟล์ในโฟลเดอร์ตัวเอง, git, เรียก php)
     ไม่ต้องใช้สิทธิ์ Administrator เลยตั้งแต่แรก — เอา self-elevate ออก
  2. แม้เอา UAC ออกแล้ว `proc_open()` ของ PHP บน Windows ยังคง "ฆ่า" child
     process ทันทีที่ request จบ (ผูกกับ Job Object) ทำให้ update.bat ไม่เคย
     รันจริงอยู่ดี — เปลี่ยนไปใช้ `wscript.exe //B` + `WScript.Shell.Run(...,
     False)` แทน (ทดสอบยืนยันแล้วว่า detach จริง รอดจาก lifecycle ของ PHP)
- เพิ่ม `task/launch_detached.vbs` (launcher ที่ทำข้อ 2)

## 2026.08.24.1554
- แก้ VERSION ที่ค้างจาก `2026.08.24` (Phase A) มา 7 commit โดยไม่เคยขยับ —
  ทำให้ปุ่ม "ตรวจสอบอัปเดต" ใช้งานไม่ได้จริงมาตลอดตั้งแต่ระบบนี้เริ่มใช้
- เปลี่ยนรูปแบบเวอร์ชันเป็น `YYYY.MM.DD.HHMM` กันปัญหาเดิมไม่ให้เกิดซ้ำ
- เพิ่มหน้า "เกี่ยวกับระบบ" (`about.php`) ในเมนู "ตั้งค่าระบบ" — รวมคู่มือติดตั้ง
  และคู่มืออัปเดตไว้ในแอปเป็น 2 แท็บ ไม่ต้องออกไปดูไฟล์แยก
- เพิ่ม `docs/update-guide.html` (คู่มืออัปเดตแยกจากคู่มือติดตั้ง) +
  ทางเลือก PowerShell สำหรับดาวน์โหลดสคริปต์ตอน bootstrap เวอร์ชันเก่า
- แก้โน้ตเก่าใน `docs/install-guide.html` ที่บอกให้ import lepto/scrub เอง
  ทั้งที่ตอนนี้ auto แล้ว

## 2026.08.24
- เพิ่มระบบอัปเดตเวอร์ชัน: ปุ่ม "ตรวจสอบ/อัปเดต" ในหน้า `settings.php`, สคริปต์ `task/update.bat` (รองรับทั้ง git และ ZIP), สำรองข้อมูลอัตโนมัติก่อนอัปเดตทุกครั้ง
- แก้ปุ่ม "Setup MedAlert_DB" ให้ deploy schema แบบ glob (`*_queue.sql`) แทน list ตายตัว — ทำให้ `lepto_queue`/`scrub_queue` ที่เคยตกหล่นถูกสร้างอัตโนมัติด้วย
- ระบบ "แก้ไขเงื่อนไขดึงข้อมูล" ครบทั้ง 10 module (covid, accident, drug, sexual, lepto, scrub, dengue, patient, fracture, pharm_lab) — แก้รหัส lab/pttype/icode/pdx/เกณฑ์ผ่านหน้าเว็บได้เอง ไม่ต้องแก้โค้ด
